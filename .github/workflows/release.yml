name: release

on:
  push:
    tags: ['v*.*.*']

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache: true

      - name: Build (all platforms)
        run: |
          mkdir -p dist
          GOOS=linux  GOARCH=amd64 go build -o dist/fleet-pulse-linux-amd64   .
          GOOS=linux  GOARCH=arm64 go build -o dist/fleet-pulse-linux-arm64   .
          GOOS=darwin GOARCH=amd64 go build -o dist/fleet-pulse-darwin-amd64  .
          GOOS=darwin GOARCH=arm64 go build -o dist/fleet-pulse-darwin-arm64  .
          GOOS=windows GOARCH=amd64 go build -o dist/fleet-pulse-windows-amd64.exe .

      - name: Checksums
        run: |
          cd dist
          sha256sum fleet-pulse-* > SHA256SUMS

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/fleet-pulse-linux-amd64
            dist/fleet-pulse-linux-arm64
            dist/fleet-pulse-darwin-amd64
            dist/fleet-pulse-darwin-arm64
            dist/fleet-pulse-windows-amd64.exe
            dist/SHA256SUMS
          generate_release_notes: true
